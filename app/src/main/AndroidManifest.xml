<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.settleup">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SettleUp">
        <activity android:name=".DivideAmountActivity">

            </activity>

        <activity android:name=".ui.SendFeedbackActivity" />
        <activity android:name=".ui.CreateGroupActivity" />
        <activity android:name=".NewExpenseActivity" >

        </activity>
         <activity android:name=".AddMembers" />
        <activity android:name=".TransactionsActivity" />
        <activity android:name=".SignInActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SettleUp.NoActionBar" />
        <activity
            android:name=".SliderActivity"
            android:theme="@style/splash_screen_theme" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/splash_screen_theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

